version: x.{build}

configuration:
- Debug
- Release

environment:
  matrix:
  - VS_VERSION: 10
    VS_PLATFORM: win32
  - VS_VERSION: 10
    VS_PLATFORM: x64

before_build:
- cd build
- premake4.exe vs2010 

build:
  project: build\waterspout.sln
  parallel: true
  verbosity: minimal
